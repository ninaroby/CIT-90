package main

import (
	"fmt"
	"math"
)

func main() {
	fmt.Printf("Now you have %g problems.", math.Sqrt(90))
}

/*This code imports using the parenthesised, factored import statement.
You can write multiple import statements like:

import "fmt"
import "math"

But it is good practice to use the factored import statement.*/
